name: Export Rust Toolchain

on:
  push:
    paths:
      - '.github/workflows/export-rust-toolchain.yml'
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Package standalone stable toolchain and source tree
        shell: bash
        run: |
          set -euo pipefail
          toolchain="$(rustup show active-toolchain | awk '{print $1}')"
          echo "$toolchain"
          tar -C "$HOME/.rustup/toolchains" -czf "$RUNNER_TEMP/rust-toolchain.tar.gz" "$toolchain"
          git archive --format=tar.gz --output="$RUNNER_TEMP/rax-plio-qdx-source.tar.gz" HEAD
          ls -lh "$RUNNER_TEMP/rust-toolchain.tar.gz" "$RUNNER_TEMP/rax-plio-qdx-source.tar.gz"
      - uses: actions/upload-artifact@v4
        with:
          name: rust-toolchain-linux-x86_64
          path: ${{ runner.temp }}/rust-toolchain.tar.gz
          compression-level: 0
          retention-days: 1
      - uses: actions/upload-artifact@v4
        with:
          name: rax-plio-qdx-source
          path: ${{ runner.temp }}/rax-plio-qdx-source.tar.gz
          compression-level: 0
          retention-days: 1
