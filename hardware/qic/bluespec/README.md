# Bluespec PLIO-QIC

Implement after the Rust QIC model and canonical traces stabilize. Use guarded atomic rules for worker cycles, bus request/grant ownership, DMA bursts, Notification, timeout, and error recovery.
